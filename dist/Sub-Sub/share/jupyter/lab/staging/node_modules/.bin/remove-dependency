#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@jupyterlab/buildutils/lib/remove-dependency.js" "$@"
  ret=$?
else 
  node  "$basedir/../@jupyterlab/buildutils/lib/remove-dependency.js" "$@"
  ret=$?
fi
exit $ret
