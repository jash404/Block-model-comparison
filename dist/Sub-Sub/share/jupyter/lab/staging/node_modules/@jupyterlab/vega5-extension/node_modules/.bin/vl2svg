#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../vega-lite/bin/vl2svg" "$@"
  ret=$?
else 
  node  "$basedir/../../../../vega-lite/bin/vl2svg" "$@"
  ret=$?
fi
exit $ret
