#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../vega-lite/bin/vl2pdf" "$@"
  ret=$?
else 
  node  "$basedir/../../../vega-lite/bin/vl2pdf" "$@"
  ret=$?
fi
exit $ret
